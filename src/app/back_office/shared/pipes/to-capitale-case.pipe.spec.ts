import { ToCapitaleCasePipe } from './to-capitale-case.pipe';

describe('ToCapitaleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ToCapitaleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
